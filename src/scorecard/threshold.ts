// The one home for the number of surf reports a spot owes before this product
// will say anything at all about how it did there (decision 19).
//
// It lives alone in its own module so the future write-path composer imports
// it. Retyping 30 at a second site is how a threshold quietly drifts from the
// sentence the surfer reads.
export const REPORTS_REQUIRED = 30;
