/**
 * Settled spot-page wording. Keep this separately so later review can swap
 * copy without coupling language changes to the browser capability boundary.
 */
export const pushCopy = {
  activate: 'Avisos de este spot',
  permissionRefused: 'Sin permiso no podemos avisarte.',
} as const;
