<!-- des-feature-context-bootstrap: {"feature_id":"f-paste-the-call-into-the-group","intent":"A surfer taps once on a spot page, pastes the day's call and a link straight into the 500-person WhatsApp group, and the link previews with the spot name and its score.","inventory":[],"schema_version":"1","state":"OPEN"} -->
# Feature context: f-paste-the-call-into-the-group

Intent: A surfer taps once on a spot page, pastes the day's call and a link straight into the 500-person WhatsApp group, and the link previews with the spot name and its score.

## Wave: DISCUSS / [REF] Slice Plan

| Slice | Value statement | Status | Annotation | Justification |
|-------|-----------------|--------|------------|---------------|
| slice-01 | A surfer on the home page taps the WhatsApp button once, WhatsApp opens with the day's call already written out in Spanish (date, winning spot, score, size, wind, window, confidence, link), they pick the group, and send. Works with JavaScript switched off. | pending | @walking_skeleton, owns the `site` config line | Thinnest end-to-end vertical: the entire feature promise minus the preview, at zero JS. The paste block is the settled template in `application-architecture.md` §10 ("Share card template"), and §13 names the `wa.me/?text=` anchor as the JS-off carrier of the same text. Placed first because `docs/DISCUSS-decisions.md` 30 says anyone posts from any page, and the home top card already ships (keystone slices 01 to 05, HANDOFF §10 base `63d5b1e`). Every field the template needs is in that spot's `days[0]` summary per `application-architecture.md` §7 P1/P7, including `conf_level`, so the `Confianza {nivel}` line renders now. **This slice owns setting `site` in `astro.config.mjs`.** Decided 2026-08-09 (HANDOFF §10, integration worktree copy): no domain is registered, build against `https://d1j9u9fxnap4es.cloudfront.net`. The `{url}?b={build_id}` line inside the pasted text needs an absolute host, `astro.config.mjs` has none today (verified, 12 lines), and setting it is one line. Every consumer of the host (share `{url}`, later `og:url`, canonical) must read the configured `site`, never a hardcoded string, so registering a real domain later is one config edit plus a republish, not rework (`docs/DISCUSS-decisions.md` 31 stays intact: register when there is something to host). **Branch declared for DISTILL:** `application-architecture.md` "What I am unsure about" records that `wa.me/?text=` with no phone number is unverified and needs one live check. This slice runs that check. If it passes, the anchor ships. If it fails, slice-02's clipboard island becomes slice-01 and the anchor is struck from the plan. DISTILL must not inherit the anchor as a settled premise. |
| slice-02 | The same call lands on the clipboard with one tap, so a surfer can paste it into the group, a DM, or anywhere else, not only through WhatsApp's own picker. | pending | depends-on slice-01 | `docs/DISCUSS-decisions.md` 5 says "One tap copies the formatted daily call plus link", and `application-architecture.md` §13 makes clipboard copy the primary action with the anchor as the JS-off fallback. Split from slice-01 because the anchor is the zero-JS floor and the island is the enhancement on top; shipping the floor first means a JS failure never removes the feature. The island is `application-architecture.md` §5 line item 3 and §6 row 2, 1.0 KB gz deferred, already inside the budget, loading after first render. The copied state and the clipboard-denied state are designed states per U5 (`nw-ui-quality-mandates`), never framework defaults. |
| slice-03 | When that link is pasted into the group, WhatsApp shows a preview naming the spot and its score instead of a bare URL. | pending | depends-on slice-01 (needs the configured `site` for absolute `og:url`) | `application-architecture.md` §13: every page carries `og:title`, `og:description`, `og:locale`. The spot name and the score live in `og:title` and `og:description`, so this slice delivers the literal feature promise with no image pipeline at all. Publish-time `<meta>` elements only, per `adr-publish-time-html-rendering.md`; no runtime service exists or is proposed (`07-write-path.md` line 90: "No share endpoint exists: decision 30's WhatsApp card is client-only composition"). The page canonical strips the `?b=` parameter (§13). `Base.astro`'s head is a named contended seam shared with the SIGNAL and READ lanes; this slice's meta block lands first and alone (BUILD-ORDER §2, contended files table). |
| slice-04 | The preview carries a real card image with the spot, the score, the size and the confidence on it, and re-pasting after a new build shows the new numbers rather than a stale card. | pending | depends-on slice-03, depends-on Pre-requisite 3 (OG image cadence) | `application-architecture.md` §13 and its Decisions-needing-Andres row 4: 1200x630 JPEG, 60 KB ceiling, JPEG not WebP because WhatsApp's WebP preview handling is not in the research corpus and a broken preview is worse than 15 KB on the sharer's side. Treated as settled. Freshness comes from `?b={build_id}` on the share URL with the page canonical stripping the parameter, which makes WhatsApp's preview-caching behaviour (also not in the corpus) irrelevant instead of assumed. Input contract is P7 in `application-architecture.md` §7, sourced from that spot's `days[0]` summary; missing fields degrade to the static generic card and log the gap, which is the declared failure behaviour, not an invention. The open decision is cadence and form: per-spot per-build (architecture recommendation, ~14,400 S3 PUTs/month at 20 spots x 24 builds, `plan-cluster-reach.md` §2.2) versus dawn-build only (BUILD-ORDER D17 recommendation, 24x fewer PUTs) versus the static generic card alone. Both per-spot and generic are shippable because generic is the automatic fallback either way; the cadence call belongs to Andres and blocks this slice's DISTILL, not the three slices before it. |
| slice-05 | A surfer on any spot page taps once and shares that spot's own call, and the link previews with that spot's name and score. | pending | depends-on keystone slice-06 | This is the feature's literal wording ("taps once on a spot page"). Held to last because the per-spot page surface belongs to keystone slice-06 (`docs/feature/daily-call-with-permanent-receipts/feature-delta.md`), in flight at workspace creation (BUILD-ORDER baseline). `docs/DISCUSS-decisions.md` 30 ("Anyone, from any spot page") is only fully satisfied when this lands; slices 01 to 04 satisfy it from the home card, which is the audience-building half the epic row is placed early for. No new mechanism: the same island, the same template, the same OG shape, scoped to one spot. Spot-detail worst case is ~51 KB of the 100 KB first-visit cap (`application-architecture.md` §5), so the 1.0 KB island fits with room. |

Notes on the plan:

- Row order is dependency order, backward only. An empty Annotation cell is parallel-safe once the rows above it have landed.
- **No server surface, by contract.** `07-write-path.md` line 90: "No share endpoint exists: decision 30's WhatsApp card is client-only composition." Every slice above is client plus builder work. A slice that provisions a backend for sharing violates the accepted design.
- **Byte discipline.** Keystone slice-08's CI gate enforces the ceilings of `application-architecture.md` §5: 14 KB gz per document, 100 KB per route first visit. Estimated adds: the `wa.me` anchor ~120 B gz on the home document (slice-01), the OG meta block ~180 B gz per document head (slice-03), the share island 1.0 KB gz deferred and already booked as budget line item 3 (slices 02 and 05), the OG image zero page bytes because only WhatsApp's crawler fetches it (slice-04). Every byte-adding slice ships under the gate; a regression fails CI naming route, measured bytes and ceiling.
- **Domain swap path.** `site` in `astro.config.mjs` is the single owner of the absolute host. The share template `{url}`, `og:url` and the canonical all derive from it. Registering surfsuppanama.com later is one config edit, one rebuild, one republish. Until then every paste carries the CloudFront hostname; that cost was accepted with the decision (HANDOFF §10).
- **UI classification.** Every slice is user-visible (slice-04's page-facing observable is the preview card). At each slice's DISTILL open, load `nw-ui-quality-mandates`, carry the U1-U7 requirement rows (R20 to R26 in `distill/requirement-checklist.md`) with executable checks through the built surface, and seal the slice charter's U8 observation for Vera. No later polish pass exists.
- **P7 field integrity, learned the hard way.** HANDOFF §10 records that `data/published-surface.json` shipped with the share-relevant optional fields missing on 19 of 20 calls while every gate stayed green. The share text and the OG card read the same five fields (`score_q`, `size_band`, `size_range_m`, `wind_state`, `conf_level`). Slice-01 DISTILL owes an explicit check that the fields the template reads are populated for the shared spot, and the declared degrade paths (P7 row, §7) are exercised, never assumed.

## Wave: DISCUSS / [REF] Definition of Done

| # | Done means |
|---|---|
| 1 | From the home card and from every spot page, one tap opens WhatsApp with the full Spanish call of the `application-architecture.md` §10 template (date, spot, score, size, wind, window, confidence, absolute link with `?b={build_id}`), and one tap copies the same text. The WhatsApp anchor works with JavaScript off. |
| 2 | A link pasted into WhatsApp previews with the spot name and its score; after slice-04, with the real card image; re-pasting after a new build shows the new numbers, never a stale card presented as fresh. |
| 3 | Every shared URL is absolute against the configured `site` (today the CloudFront hostname). Swapping in a registered domain is one `astro.config.mjs` edit plus a republish; no template or component rework. |
| 4 | No share server surface exists: composition is client-only per `07-write-path.md` line 90. No new endpoint, Lambda, or write path was added by this feature. |
| 5 | Byte gate green: home document at or under 14 KB gz, every route at or under 100 KB first visit, share island at or under 1.0 KB gz deferred (budget line item 3, `application-architecture.md` §5). |
| 6 | U1-U7 checks green per visible slice through the built surface, and a sealed source-blind Vera PASS against each slice charter's U8 observation: 390 px, WCAG-AA against the real backdrop in both themes, reduced-motion honoured, 44 px targets. |
| 7 | Zero technical text in the pasted message and the preview: no model names, JSON, placeholder tokens, or English on the Spanish surface. |
| 8 | Every Slice Plan row above is flipped `shipped`. |

## Wave: DISCUSS / [REF] Out-of-scope

| Out | Lands in |
|---|---|
| English share card, `og:locale` en alternate, English push copy | F-READ-IT-IN-YOUR-LANGUAGE (READ-06 translates this feature's template and OG shape after they exist) |
| Share prompt on the reveal screen ("Pásalo al grupo", `application-architecture.md` §10) | F-TELL-US-WHAT-YOU-SAW-COLD's surface, reusing this feature's island |
| Web push, per-spot subscriptions | F-TELL-ME-WHEN-ITS-WORTH-THE-DRIVE |
| Posting into the group by API or bot | Never. `docs/research/raw/12-community-whatsapp-ugc.md` §1: no official API can join, read or post into a pre-existing consumer group; §2: unofficial clients are a ToS violation with a ban expectancy. The product surface is a human pasting. |
| Domain registration | Andres (`docs/DISCUSS-decisions.md` 31). This feature consumes whatever `site` says; the swap is config-only per Done row 3. |
| Share analytics or click tracking | Never (BRIEF constraint 3; `application-architecture.md` §5 cut table: no analytics scripts). |

## Wave: DISCUSS / [REF] Pre-requisites

| # | Pre-requisite | Blocks | Owner | Status |
|---|---|---|---|---|
| 1 | Absolute site host for the `{url}` line and `og:url` | nothing any more | Andres | ANSWERED 2026-08-09 (HANDOFF §10): no domain yet, build against `https://d1j9u9fxnap4es.cloudfront.net`; slice-01 sets `site` in `astro.config.mjs`. Known cost, recorded: the CloudFront hostname appears in every paste until the domain lands (BUILD-ORDER D1, option b chosen). |
| 2 | `wa.me/?text=` with no phone number verified live (`application-architecture.md` "What I am unsure about") | the anchor half of slice-01 only | slice-01 DISTILL/DELIVER | open, resolves inside slice-01; failure branch declared in the slice row (clipboard island becomes the floor) |
| 3 | OG image cadence and form: per-spot per-build (`application-architecture.md` Decisions needing Andres 4, recommendation A) vs dawn-build only (BUILD-ORDER D17 recommendation, 24x fewer S3 PUTs) vs static generic card only | slice-04 DISTILL | Andres | open. Both per-spot and generic are shippable; the static generic card is the automatic fallback either way (P7 failure behaviour, `application-architecture.md` §7). The call sets builder PUT volume, not page bytes. |
| 4 | Keystone slice-06 (per-spot pages) shipped | slice-05 | keystone lane | in flight at workspace creation (BUILD-ORDER baseline: 06, 07, 08 landing shortly) |
| 5 | Keystone slice-08 byte gate green in CI | nothing from building; every byte-adding slice ships under it | keystone lane | in flight at workspace creation |
| 6 | Published-surface share fields populated per build: `score_q`, `size_band`, `size_range_m`, `wind_state`, `conf_level` plus `call{es}` (P1/P7, `application-architecture.md` §7), with the producer guard HANDOFF §10 names after the 19-of-20 missing-fields failure | slice-01 honesty, not its build | producer lane | open |
